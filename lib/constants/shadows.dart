import 'package:flutter/material.dart';

List<BoxShadow> everythngComponentShadow = [
   BoxShadow(
    color: Colors.black.withOpacity(0.07),
    blurRadius: 80,
     offset: const Offset(0,100),
  ),
  BoxShadow(
    color: Colors.black.withOpacity(0.0247),
    blurRadius: 33.42,
     offset: const Offset(0,22.33),
  ),
  BoxShadow(
    color: Colors.black.withOpacity(0.0199),
    blurRadius: 17.87,
     offset: const Offset(0,8.67),
  ),
  BoxShadow(
    color: Colors.black.withOpacity(0.0205),
    blurRadius: 10.02,
     offset: const Offset(0,3.28),
  ),
  BoxShadow(
    color: Colors.black.withOpacity(0.0221),
    blurRadius: 5.32,
     offset: const Offset(0,0.87),
  ),
  BoxShadow(
    color: Colors.black.withOpacity(0.02047),
    blurRadius: 2.21,
     offset: const Offset(0,-0.02),
  ),

];